services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 003-api
    environment:
      APP_DEBUG: "false"
    ports:
      - "8000:8000"
    restart: unless-stopped
