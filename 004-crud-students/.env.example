# Postgres credentials consumed by docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=students

# Application settings (prefix APP_ comes from pydantic-settings env_prefix)
APP_DEBUG=false
APP_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/students
APP_DATABASE_URL_TEST=postgresql://postgres:postgres@localhost:5432/students_test
