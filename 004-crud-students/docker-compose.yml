services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 004-crud-students
    environment:
      APP_DEBUG: "false"
    ports:
      - "8000:8000"
    restart: unless-stopped
